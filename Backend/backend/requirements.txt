fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
firebase-admin>=6.5.0
cloudinary>=1.40.0
slowapi>=0.1.9
httpx>=0.27.0
python-dotenv>=1.0.0

# ── Magic bytes MIME detection ─────────────────────────────────────────────────
python-magic-bin>=0.4.14; sys_platform == "win32"
python-magic>=0.4.27; sys_platform != "win32"

# ── Computer Vision & Image Processing ─────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.8.0

# ── Audio & Signal Processing Forensics ────────────────────────────────────────
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.12.0

# ── Optional Heavy ML Inference Packages ──────────────────────────────────────
# On Linux/Docker or CUDA environments, deep learning models run via:
# torch>=2.2.0
# torchvision>=0.17.0
# transformers>=4.40.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
